# 数据库初始化和配置说明

## ⚠️ 重要提示

由于现在才执行数据库初始化，您需要按照以下步骤操作：

---

## 📋 执行步骤

### 步骤 1: 在 Supabase 执行初始化脚本

1. 打开 Supabase Dashboard: https://app.supabase.com/project/wlksiulicosdnerzhkdl

2. 点击左侧菜单 **SQL Editor**

3. 复制 `/scripts/init-database.sql` 的**全部内容**

4. 粘贴到 SQL Editor 并点击 **Run**

5. 验证执行成功：
   - ✅ 看到 "Success. No rows returned"
   - ✅ 没有错误信息

### 步骤 2: 验证表已创建

在 SQL Editor 中执行：

\`\`\`sql
-- 查看 brand_config 表
SELECT * FROM brand_config;

-- 查看 brand_presets 表
SELECT * FROM brand_presets;
\`\`\`

应该看到：
- `brand_config` 表有 1 行数据（默认配置）
- `brand_presets` 表有 2 行数据（fxkiller 和 testbrand）

### 步骤 3: 重启开发服务器

```bash
# 停止当前服务器 (Ctrl+C)
# 然后重新启动
pnpm dev
```

### 步骤 4: 验证配置加载

访问 http://localhost:3000

检查：
- ✅ 页面正常加载，无错误
- ✅ 导航栏显示 "汇刃"
- ✅ Footer 左下角显示邮箱图标
- ✅ Footer 右侧显示推荐码（Binance、FundedNext、EC Markets、TickMill）
- ✅ Footer 底部显示 FTMO 和 TickMill 横幅

---

## 🔍 当前配置内容

初始化脚本包含的默认配置：

### 品牌信息
- 品牌名称（中文）：汇刃
- 品牌名称（英文）：FX Killer
- 域名：fxkiller.com
- 全局邮箱：x.stark.dylan@gmail.com

### 联系方式（Footer 左下角）
- ✅ 邮箱（启用）- 点击显示面试预约弹窗
- ❌ 微信（禁用）
- ❌ Telegram（禁用）
- ❌ 电话（禁用）

### 推荐码（Footer 右侧）
1. **Binance**
   - 邀请码：71591417
   - 优惠：5%优惠

2. **FundedNext**
   - 邀请码：fx_killer
   - 优惠：5%优惠

3. **EC Markets**
   - 邀请码：99R9C
   - 优惠：全返

4. **TickMill**
   - 邀请码：IB47958600
   - 优惠：全返

### Footer 横幅（底部）
1. **FTMO**
   - 图片：/brand/partners/ftmo-banner.png
   - 链接：https://ftmo.com/?affiliates=UUdNjacFYttdgsZcEozt

2. **TickMill**
   - 图片：/brand/partners/tickmill-banner.png
   - 链接：https://www.tickmill.com

---

## ⚙️ 如何修改配置

### 方法一：在 Supabase 直接编辑

1. 打开 Supabase Dashboard
2. 点击 **Table Editor**
3. 选择 `brand_config` 表
4. 点击编辑图标修改字段
5. 保存后等待 5 分钟缓存过期，或重启服务器

### 方法二：使用 Dashboard（推荐）

1. 访问 http://localhost:3000/dashboard
2. 输入密码：`123456`
3. 点击左侧 "🎨 品牌配置"
4. 查看当前配置
5. 使用品牌预设切换功能快速测试

---

## 📝 注意事项

### 关于横幅图片

初始化脚本中配置的横幅图片路径为：
- `/brand/partners/ftmo-banner.png`
- `/brand/partners/tickmill-banner.png`

**您需要**：
1. 准备横幅图片（推荐尺寸：高度 90px，宽度自适应）
2. 将图片放到 `/public/brand/partners/` 目录
3. 或修改 Supabase 中的 `footer_banners` 字段，使用在线图片链接

### 关于合作伙伴模块

主页的合作伙伴模块（PartnersLogos）目前是**硬编码**的，包含：
- 10 个合作伙伴 Logo（/public/logos/1-10.png）
- 5 个推荐链接（EC Markets、TickMill、Binance、FTMO、FundedNext）

**未来可以改造成配置驱动**，但目前需要修改代码。

### 关于联系方式

如果需要启用微信联系方式：

1. 在 Supabase 的 `brand_config` 表中找到 `contact_methods` 字段
2. 编辑 JSON，找到 `"type": "wechat"` 的对象
3. 修改：
   - `"enabled": true` - 启用显示
   - `"value": "your_wechat_id"` - 你的微信号
   - `"qrcode_url": "https://..."` - 微信二维码图片链接（可选）
4. 保存

---

## ✅ 完成后的验证清单

- [ ] 数据库初始化脚本执行成功
- [ ] brand_config 表有数据
- [ ] brand_presets 表有数据
- [ ] 开发服务器已重启
- [ ] 首页正常显示
- [ ] Footer 左下角有邮箱图标
- [ ] Footer 右侧显示 4 个推荐码
- [ ] Footer 底部显示 2 个横幅（需要图片）
- [ ] Dashboard 登录密码是 123456
- [ ] Dashboard 品牌配置页面显示正确

现在请按照步骤执行数据库初始化！🚀
